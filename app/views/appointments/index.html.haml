- if user_signed_in?
  = current_user.email
  = link_to 'Sign out', destroy_user_session_path, method: :delete
= react_component 'AppRouter'

= javascript_pack_tag 'appointments-bundle'
= stylesheet_pack_tag 'appointments-bundle'
